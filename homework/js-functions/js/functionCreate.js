// ! Declaration (оголошення)
function testAddition(a, b) {
  return a + b;
}

// ! Присвоєння (Expression)
const testDivision = function testDivision(a, b) {
  return a / b;
};

// ! Стрілочна

const multipyTest = (a, b) => {
  return a * b;
};

export default testAddition;
